import React from 'react';
import NavBar from '../components/NavBar';

const CartPage = () => {
    return (
        <div>
            <NavBar/>
            CartPage
        </div>
    );
};

export default CartPage;